# Mesh-Network-Communication
ESPNowMeshFull is a multi-hop ESP-NOW mesh networking framework for ESP32. It supports TTL-based routing, RSSI-based path selection, loop prevention, neighbor discovery, and reliable messaging with ACKs and retries. A built-in serial terminal allows easy control, testing, and monitoring of the mesh network.
